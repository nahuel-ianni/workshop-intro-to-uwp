<Page
    x:Class="BackgroundAgent.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BackgroundAgent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Width" Value="225"/>
            <Setter Property="Height" Value="35"/>
        </Style>    
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock x:Uid="SimpleBackgroundTaskContent" Grid.Column="0" Grid.Row="0"/>
        <TextBlock x:Name="SimpleBackgroundTaskContent" Grid.Column="1" Grid.Row="0"/>

        <TextBlock x:Uid="AsyncBackgroundTaskContent" Grid.Column="0" Grid.Row="1"/>
        <TextBlock x:Name="AsyncBackgroundTaskContent" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</Page>
